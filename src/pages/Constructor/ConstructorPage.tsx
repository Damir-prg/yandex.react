import { PageContainer } from "pages/ui";

import classes from "./constructorPage.module.css";

export const ConstructorPage = () => {
  return (
    <PageContainer className={classes["constructor-page"]}></PageContainer>
  );
};
